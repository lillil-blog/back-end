{
    "apps": [
        {
            "name": "DSTB_Test",

            "exec_mode": "cluster",

            "script": "dist/main.js",

            "autorestart": true,

            "watch": true,
            "watch_options": {
                "ignore_watch": ["node_modules", "logs", "public"]
            },

            "restart_delay": 60000
        }
    ]
}
